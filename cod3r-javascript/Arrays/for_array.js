const numeros = [1, 2, 3, 4, 5, 6, 7]
                 //acessano valores indefinidos nos array
// numeros.length -> faz a contagem dos dados mesmo tendo sido mudado a quantidade de posições

//    variavel   expresão    incrementação
for(let i = 0;i < numeros.length; i++){
    console.log(i, numeros[i])//acessando os valores do array
}